# 音视频同步配置说明

## 概述

当前代码已经实现了完整的音视频同步机制，**默认使用音频同步**（视频同步到音频时钟），并支持通过配置文件自定义同步策略。

## 同步策略

### 1. 音频同步 (Audio Master) - **默认**
- **模式值**: `audio` 或不配置
- **行为**: 视频同步到音频时钟
- **适用场景**: 
  - 标准视频播放（推荐）
  - 音质优先
  - 大多数场景的最佳选择

### 2. 视频同步 (Video Master)
- **模式值**: `video`
- **行为**: 音频同步到视频时钟（当前版本未实现音频速率调整）
- **适用场景**: 
  - 无音频视频
  - 画面流畅度优先
  - 视频演示/监控

### 3. 外部时钟 (External Clock)
- **模式值**: `external`
- **行为**: 音视频同步到外部时钟（当前版本等同于音频同步）
- **适用场景**: 
  - 实时流媒体
  - 多媒体同步需求

## 配置方法

### 在配置文件中设置

编辑配置文件（通常是 `etc/hplayer.ini` 或类似文件），在 `[video]` 节下添加：

```ini
[video]
# 音视频同步模式
# audio  - 音频同步（默认，推荐）
# video  - 视频同步
# external - 外部时钟同步
av_sync_type=audio
```

### 配置示例

#### 示例1: 音频同步（默认）
```ini
[video]
av_sync_type=audio
```
或直接不配置该项（使用默认值）。

#### 示例2: 视频同步
```ini
[video]
av_sync_type=video
```

#### 示例3: 外部时钟
```ini
[video]
av_sync_type=external
```

## 同步算法

### 参考ffplay实现

当前实现参考了ffplay的 `compute_target_delay()` 函数，包括：

1. **同步阈值计算**
   - 最小阈值: 40ms (AV_SYNC_THRESHOLD_MIN)
   - 最大阈值: 100ms (AV_SYNC_THRESHOLD_MAX)
   - 动态调整阈值，基于帧间隔

2. **延迟调整策略**
   ```
   如果 video_clock < audio_clock - threshold:
       减少延迟（加速视频，追赶音频）
   
   如果 video_clock > audio_clock + threshold:
       增加延迟（减速视频，等待音频）
       或重复帧（2x延迟）
   
   如果差异在阈值内:
       保持正常延迟
   ```

3. **防止过度同步**
   - 最大允许差异: 10秒 (AV_NOSYNC_THRESHOLD)
   - 超过此值认为时间戳不连续，不做同步

## 日志输出

启动时会显示：
```
AV sync type: 0 (audio master)
```

播放时会显示前5帧的同步信息：
```
A-V sync: diff=0.023, delay=0.033->0.025
A-V sync: diff=-0.012, delay=0.033->0.045
```

其中：
- `diff`: 视频时钟 - 音频时钟的差值（负值表示视频落后）
- `delay`: 原始帧延迟 -> 调整后的延迟

## 自动降级

### 无音频流
如果视频文件没有音频流，系统会自动：
1. 检测到 `audio_stream_index=-1`
2. 自动切换到视频同步模式
3. 日志显示: "No audio stream or audio initialization failed, using video clock as master"

### 无视频流
如果只有音频流，系统会自动使用音频时钟。

## 性能特点

### 优点
- ✅ 精确的音视频同步（±40ms内）
- ✅ 动态延迟调整，适应网络抖动
- ✅ 支持快进/快退后的快速重新同步
- ✅ 帧率自适应

### 当前限制
- ⚠️ 视频同步模式下，音频不会调整采样率（未来改进）
- ⚠️ 外部时钟目前等同于音频时钟（未来可扩展）

## 调试建议

### 检查同步质量

1. **观察日志**
   ```
   A-V sync: diff=0.023, delay=0.033->0.025
   ```
   - diff应该在±50ms内
   - delay调整应该平滑

2. **听觉测试**
   - 播放有明显节奏的视频（如音乐视频）
   - 声音和画面应该同步

3. **视觉测试**
   - 播放说话的视频
   - 嘴唇动作和声音应该匹配

### 常见问题

#### 问题1: 音视频不同步
**可能原因**:
- 视频文件时间戳不连续
- 解码速度不够
- 系统负载过高

**解决方案**:
- 检查CPU使用率
- 尝试关闭硬件解码
- 降低视频分辨率

#### 问题2: 视频卡顿
**可能原因**:
- 同步调整过于频繁
- 帧延迟计算异常

**解决方案**:
- 检查日志中的delay值
- 确认视频帧率正常

## 代码位置

### 关键函数
- `get_master_sync_type()` - 确定当前同步模式 (hffplayer.cpp:1221)
- `get_master_clock()` - 获取主时钟 (hffplayer.cpp:1256)
- `compute_target_delay()` - 计算调整后的延迟 (hffplayer.cpp:1272)
- `processVideoPacket()` - 应用同步逻辑 (hffplayer.cpp:1018)

### 配置读取
- 构造函数中读取配置 (hffplayer.cpp:148-159)

## 未来改进

1. **音频速率调整**
   - 在视频同步模式下调整音频采样率
   - 参考ffplay的 `synchronize_audio()`

2. **真正的外部时钟**
   - 实现独立的外部时钟源
   - 支持网络时间同步

3. **自适应同步阈值**
   - 根据网络状况动态调整阈值
   - 学习算法优化同步参数

4. **帧丢弃策略**
   - 在CPU负载高时智能丢帧
   - 保持音视频同步

## 总结

✅ **当前状态**: 已实现音频同步，默认开启，可配置  
✅ **参考标准**: ffplay同步算法  
✅ **配置方式**: 通过配置文件 `av_sync_type` 设置  
✅ **自动适应**: 无音频时自动切换到视频同步  

推荐使用默认的音频同步模式，适用于绝大多数场景。

